name: Repo Push Check

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Show repo structure
      run: |
        echo "Repository structure:"
        ls -R | head -200

